<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title or "Sebastian Meckovski - Coming Soon" }}</title>
        <link rel="preload" href="../assets/fonts/goldman/Goldman-Regular.ttf" as="font" type="font/ttf" crossorigin>
        <link rel="preload" href="../assets/fonts/goldman/Goldman-Bold.ttf" as="font" type="font/ttf" crossorigin>
        {% if env.ENVIRONMENT == "development" %}
            <script src="https://cdn.tailwindcss.com"></script>
            <link href="../assets/css/styles.css" rel="stylesheet">
        {% else %}
            <link href="../assets/css/output.css" rel="stylesheet">
        {% endif %}
    </head>
    <body class="bg-gray-800 text-white flex flex-col min-h-screen">
        <nav class="p-4 relative">
            <div class="container mx-auto flex justify-between items-center">
                <div class="flex items-center text-[var(--text-header)] text-2xl md:text-2xl font-bold">
                    <a href="/">Sebastian
                        <span class="text-[var(--accent-color)]">Meckovski</span>
                    </a>
                    <button
                        id="theme-toggle"
                        class="ml-4 px-2 py-1 rounded bg-[var(--btn-bg)] text-[var(--btn-text)] border border-[var(--accent-color)] hover:bg-[var(--btn-bg-hover)] transition-colors duration-200 text-sm">
                        Toggle Theme
                    </button>
                </div>
                <!-- Mobile menu button -->
                <div class="md:hidden flex items-center justify-center">
                    <button
                        id="mobile-menu-button"
                        class="text-[var(--nav-icon)] hover:text-[var(--nav-icon-hover)] focus:outline-none focus:text-[var(--nav-icon-hover)] flex items-center justify-center">
                        <svg class="h-6 w-6 fill-current" viewbox="0 0 24 24">
                            <path
                                class="hidden"
                                d="M18.278 16.864a1 1 0 0 1-1.414 1.414l-4.829-4.828-4.828 4.828a1 1 0 0 1-1.414-1.414l4.828-4.829-4.828-4.828a1 1 0 0 1
                                    1.414-1.414l4.829 4.828 4.828-4.828a1 1 0 1 1 1.414 1.414l-4.828 4.829 4.828 4.828z"/>
                            <path d="M4 5h16a1 1 0 0 1 0 2H4a1 1 0 1 1 0-2zM4 11h16a1 1 0 0 1 0 2H4a1 1 0 0 1 0-2zM4 17h16a1 1 0 0 1 0 2H4a1 1 0 0 1 0-2z"/>
                        </svg>
                    </button>
                </div>
                <!-- Desktop menu -->
                <div class="hidden md:flex space-x-4">
                    {% for item in navigation %}
                        <a href="{{ item.url }}" class="nav-link text-[var(--nav-link)] hover:text-[var(--nav-link-hover)] px-3 py-1 rounded transition-colors duration-200 {% if page.url == item.url %}bg-[var(--accent-color)]/20{% endif %}">{{ item.text }}</a>
                    {% endfor %}
                </div>
            </div>
            <!-- Mobile menu -->
            <div
                id="mobile-menu"
                class="md:hidden absolute top-full left-0 right-0 bg-[var(--bg-nav)] shadow-lg opacity-0 invisible transform -translate-y-4
                    transition-all duration-200 ease-in-out z-50">
                <div class="flex flex-col space-y-2 p-4">
                    {% for item in navigation %}
                        <a href="{{ item.url }}" class="nav-link text-[var(--nav-link)] hover:text-[var(--nav-link-hover)] px-3 py-2 rounded transition-colors duration-200 {% if page.url == item.url %}bg-[var(--accent-color)]/20{% endif %}">{{ item.text }}</a>
                    {% endfor %}
                </div>
            </div>
        </nav>
        <main id="swup" class="flex-grow transition-fade">
            {{ content | safe }}
        </main>
        <footer class="p-4 text-center text-[var(--footer-text)]">
            <p>&copy;
                {% if env.ENVIRONMENT == "development" %}2025 Sebastian Meckovski. Development{% else %}2025 Sebastian Meckovski. All Rights Reserved.{% endif %}
            </p>
        </footer>
        <script src='../assets/js/swup.min.js'></script>
        <script src="../assets/js/layout.js"></script>
    </body>
</html>