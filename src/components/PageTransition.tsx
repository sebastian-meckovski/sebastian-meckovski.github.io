// Deprecated: JS-based page transition removed to preserve full SSR.
// File kept intentionally empty so imports would fail (none remain) and
// bundler can tree-shake it. Safe to delete manually if desired.

export {}; // makes this a module without adding runtime
